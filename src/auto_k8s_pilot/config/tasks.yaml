# research_task:
#   description: >
#     Conduct a thorough research about {topic}
#     Make sure you find any interesting and relevant information given
#     the current year is {current_year}.
#   expected_output: >
#     A list with 10 bullet points of the most relevant information about {topic}
#   agent: researcher

# reporting_task:
#   description: >
#     Review the context you got and expand each topic into a full section for a report.
#     Make sure the report is detailed and contains any and all relevant information.
#   expected_output: >
#     A fully fledged report with the main topics, each with a full section of information.
#     Formatted as markdown without '```'
#   agent: reporting_analyst

k8s_pods_overview:
  description: >
    Call kubectl_tool with action="get", kind="pods". 
    Always return ONLY the raw output from kubectl_tool. 
    Do not reformat or invent anything. 
    If kubectl_tool returns empty, output must be empty too.
  expected_output: >
    Exact stdout from kubectl_tool (wide format). No fabricated data.
  agent: k8s_operator
  inputs:
    namespace: all

# k8s_error_logs:
#   description: >
#     If any pods look unhealthy, call kubectl_tool(action="logs") for the first one.
#     Tail {tail} lines. Summarize top 5 repeating errors.
#   expected_output: >
#     Bullet list of the top errors with counts + 10-line excerpt.
#   agent: k8s_operator
#   inputs:
#     tail: 300
#     namespace: default

